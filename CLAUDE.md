# kg

Deterministic file-level knowledge graph for codebase context retrieval.

## Running tests

The test suite only works inside the `kg` conda environment. Activate it first:

```bash
source activate kg
```
